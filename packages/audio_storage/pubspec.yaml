name: app_lib_audio_storage
description: Audio file storage module — uploads to Supabase Storage with signed URLs.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.7.0
  flutter: ">=3.29.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.0.0
  app_lib_core:
  supabase_flutter: ^2.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  mocktail: ^1.0.0
