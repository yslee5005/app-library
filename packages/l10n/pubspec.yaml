name: app_lib_l10n
description: App Library l10n package — relative time and number formatters.
version: 1.0.0
publish_to: none
environment:
  sdk: ^3.7.0
resolution: workspace
dependencies:
  app_lib_core:
dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
