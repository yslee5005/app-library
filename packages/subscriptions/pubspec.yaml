name: app_lib_subscriptions
description: Subscription management — abstract interface, status enum, mock + RevenueCat implementations.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.7.0

resolution: workspace

dependencies:
  app_lib_core:

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
