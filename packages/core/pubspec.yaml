name: app_lib_core
description: Pure Dart core library for App Library. Errors, models, utils. Zero dependencies.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.7.0

resolution: workspace

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
