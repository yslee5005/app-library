name: app_lib_pagination
description: Cursor-based pagination models and interfaces for App Library.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.7.0
  flutter: ">=3.29.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.0.0
  app_lib_core:

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
