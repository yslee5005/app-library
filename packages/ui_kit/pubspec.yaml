name: app_lib_ui_kit
description: App Library UI Kit — reusable widgets for navigation, onboarding, profile, and feed.
version: 1.0.0
publish_to: none
environment:
  sdk: ^3.7.0
  flutter: ">=3.29.0"
resolution: workspace
dependencies:
  flutter:
    sdk: flutter
  app_lib_core:
    path: ../core
  app_lib_theme:
    path: ../theme
dev_dependencies:
  flutter_test:
    sdk: flutter
