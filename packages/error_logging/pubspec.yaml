name: app_lib_error_logging
description: Error logging with sensitive data filtering for App Library.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.7.0

resolution: workspace

dependencies:
  app_lib_core:
    path: ../core

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
